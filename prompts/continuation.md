从玩家改写点之后重写故事，并判定结局。

固定前文，绝对不可改：{{fixed_front}}
固定特殊情节前半段，绝对不可改：{{fixed_editable_text}}
原剩余特殊情节：{{old_remainder_text}}
原后文（只用于理解旧失败原因，禁止照抄或沿用为新后文模板）：{{old_suffix_text}}
原判定：{{original_ending_type}} / {{original_ending_judgement}}
本次改写：{{change_record_json}}
本次改写来源：{{change_source}}
本次续写指令：{{continuation_directive}}
全部改写：{{all_changes_json}}
主角：{{protagonist_names}}

返回要求：
1. 只返回 rewrittenEditableParts、rewrittenSuffix、endingType、endingJudgement、changeSummary。
2. 玩家已经改写了唯一关键台词；从改动点之后的新内容不再属于关键情节范围。rewrittenEditableParts 必须返回 []，不要再生成新的 quote、replacementOptions、role 或 item。
3. rewrittenSuffix 尽量控制在 {{suffix_target}} 字左右，并尽量接近原后文长度；完整故事总长尽量保持在 {{full_story_min}}-{{full_story_max}} 字之间，避免越续写越长。必须从“{{old_value}} -> {{new_value}}”出发重新设计后续因果；原后文只可作为旧失败参考，不可照搬。
4. 续写里的说话逻辑必须清楚：如果新台词由 A 说出，后续回应应来自 B、其他角色或环境事件；不要写成 A 说完后又由 A 回应/回答自己。若需要 A 继续行动，写动作，不写成“回应自己的台词”。
5. 保持前文的题材、世界规则、时代质感和力量体系，不要把武侠突然改成科幻，也不要把写实突然改成魔法。
6. 后续因果必须继续以主角为中心：新台词造成的变化要落到主角的选择、困境、代价或获救可能上；不要把结局改成无关配角自己的胜负。
7. 不要在 rewrittenSuffix 里制造新的可改写台词按钮；即使出现普通对白，也只作为普通叙事文本。
8. 原剩余特殊情节目标约 {{old_remainder_length}} 字，原 quote 数量 {{quote_count}} 仅供理解原结构，不需要保留。
9. 严禁重复文字：rewrittenSuffix 内不要连续或间隔重复同一句、同一段、同一组动作；不要原样复制“原剩余特殊情节”或“原后文”的整句。必须改写、压缩、承接，而不是粘贴两遍。
10. 如果必须保留原后文中的信息，只保留一次，并用新的因果顺序表达。
11. 新台词必须造成至少两个清晰可见的后续事件变化，例如角色反应、行动选择、道具使用、危机触发方式、救援时机、证据命运、代价变化等。不能只把新台词插入旧后文，然后让旧后文照常发生。
12. 即使 endingType 仍是 bad，也必须写出“因为新台词产生了哪些变化，但为什么仍失败”的新失败链路；不要写“结局未变”式泛泛判断。
13. 改写后的结局文本必须短促清楚，优先贴近改写前后文长度；当前固定部分约 {{fixed_length}} 字，续写后完整故事目标为 {{full_story_min}}-{{full_story_max}} 字。rewrittenSuffix 参考中文小说排版，可用自然换行分段；不要使用 Markdown、编号、标题或项目符号。
14. good 是严格成功，不是“局面变好一点”。只有同时满足三项，才可判 good：新台词明确命中核心危机或关键规则；rewrittenSuffix 写出主角据此采取了具体可执行行动；核心危机被完整解决且没有明显后患。
15. 不要为了给 good 而临时发明新规则、新帮手、新道具、新权限或新背景。good 必须回收固定前文、原特殊情节或玩家新台词中已经存在的信息；如果成功需要补一个前文没有的机制，应判 neutral 或 bad。
16. 以下情况不能判 good：普通宣言、道歉、安抚、威胁、表白、喊口号、含糊承诺、只请求相信、只拖延时间、只让反派犹豫、只让系统短暂停顿、只救下一部分人/物、只得到线索但危机未解除。
17. neutral 是常见的“部分有效”结果：新台词缓和局势、争取时间、救下一部分人/物、保护部分证据、让主角暂时脱身、留下明显隐患，或需要后续行动才能真正解决核心危机。
18. bad 用于新台词明显空泛、纯情绪化、搞错目标、违背世界规则、只开玩笑且无法由前文合理化、拖延、激怒对手，或没有改变核心危机的情况。如果不确定是否足以完整解决核心危机，优先判 neutral；如果没有实际改变，判 bad。
19. 不要根据候选序号或候选类型决定结局；同话题反义、换话题、荒诞搞笑三类都可能导向 bad、neutral 或 good，只按新台词在当前故事里的因果效果判定。
20. endingType 只能是 bad、neutral、good：bad/neutral 都算失败，good 算成功。
21. endingJudgement 和 changeSummary 都用一句短句，并说明新台词造成的具体变化和最终判定理由。
